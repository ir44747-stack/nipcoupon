.shots/
.DS_Store
*.log
node_modules/
# secrets must never be uploaded
.env
.env.*
# local backups and raw network snapshots
backups/
logs/
# runtime lock
.cj-fetch.lock

# Vercel auto-builds any root middleware.js. This one imports `next/server`,
# which does not resolve on a framework:null project and fails the whole build.
middleware.js
